---
import { Image } from 'astro:assets';

export interface Props {
  src: string;
  alt: string;
}
const { src, alt } = Astro.props;
---

<figure>
  <Image {src} alt={alt} inferSize />
  <figcaption>{alt}</figcaption>
</figure>
